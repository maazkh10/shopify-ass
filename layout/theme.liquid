<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">

    <meta
      name="viewport"
      content="width=device-width, initial-scale=1"
    >

    <title>
      {{ page_title }}
    </title>

    {{ content_for_header }}

    {{ 'test-1.css' | asset_url | stylesheet_tag }}
  </head>

  <body>
    {{ content_for_layout }}

    <script src="{{ 'test-1.js' | asset_url }}" defer></script>
  </body>
</html>